name: notebook
channels:
  - eoas_ubc
  - conda-forge
dependencies:
  - base-notebook=2020.09.05
  # NOTE: pip required for running tests in this repo
  - python=3.8
  - pip=20
  - numpy
  - scipy
  - matplotlib
  - python-pptx
  - conda-lock
# conda-lock -f environment.yml -p linux-64
# conda create --name convert --file conda*lock*yml
# conda activate convert
